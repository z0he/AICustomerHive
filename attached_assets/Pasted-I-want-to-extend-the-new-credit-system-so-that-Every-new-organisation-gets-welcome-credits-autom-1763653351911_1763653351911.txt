I want to extend the new credit system so that:

Every new organisation gets welcome credits automatically.

We support referral bonuses for growth.

Everything is idempotent (no double-grants).

Please implement the following:

A. Env variables

Add and use these env vars:

FREE_STARTER_CREDITS = 200

REFERRAL_BONUS_REFERRER = 100

REFERRAL_BONUS_INVITEE = 100

B. Welcome credits on first org setup

Find the code path where:

a new user + organisation are created, OR

an organisation is first created for a user.

After the org is created:

If there is no row in the credits table for that organization_id:

Insert a credits row with balance = FREE_STARTER_CREDITS.

Insert a row in credit_transactions:

organization_id = org id

credits = FREE_STARTER_CREDITS

type = 'welcome_bonus'

source = 'system'

description = 'Welcome credits for new AICRM account'

Make this idempotent:

Either by checking credits row existence,

Or by adding a boolean flag on the org (e.g. has_received_welcome_credits) and guarding on it.

C. Referral credits

I want a simple backend-ready referral system:

Each user can have an optional referral_code value (string) attached to their account or organisation.

New signups can optionally pass referralCode in the registration payload.

Implement:

A helper to generate a unique referral code for each org (e.g. short alphanumeric).

Store it on the organisation row, e.g. referral_code.

On new org creation:

If the payload contains referralCode:

Look up the referrer organisation by that code.

If found and not already rewarded for this invitee:

Add REFERRAL_BONUS_REFERRER credits to the referrer org.

Add REFERRAL_BONUS_INVITEE credits to the new org.

Create credit_transactions rows for both:

type = 'referral_bonus'

source = 'system'

Descriptions like:

'Referral bonus – invited new organisation'

'Referral bonus – joined via referral'

Ensure idempotency so the same invitee cannot trigger bonuses multiple times:

simplest: store referred_by_org_id on the invitee org and only apply bonuses if it is null.

D. Integration with /api/credits + Usage & Billing UI

/api/credits must already be the single source of truth.
Ensure welcome + referral transactions show up correctly in:

balance

totalPurchasedCredits vs “system granted” credits (if you separate them)

transactions list.

In the Usage & Billing tab:

If totalPurchasedCredits is 0 but balance > 0, show a subtle message:

“You’re using welcome/referral credits. Top up to keep AICRM running once they run out.”

E. Logging & tests

Add clear logs when:

welcome credits are granted

referral bonuses are granted (with org ids and amounts)

Add at least one small test or script/path I can hit to verify:

new org → gets FREE_STARTER_CREDITS

org B signs up with referralCode from org A → both orgs see correct bonuses in /api/credits.

Please show me:

The main files you will change / changed

Any new env vars that must be set

Any manual steps to test this end-to-end.
Please sure to set up guardrails for existing work that has been completed on the credits / stripe system. Everything is working fine and I dont want to damage that or overwrite. Also please include a summary or breakdown on the current state and set up of the credits system, files, where critical code is etc

Give me your plan wait for approval