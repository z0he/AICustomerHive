The plan you just proposed for welcome credits + referrals looks good. Please go ahead and IMPLEMENT it, with these explicit constraints:

1) Referral and welcome logic:
   - Referral codes are PER ORGANISATION, not per user.
   - Every NEW organisation should:
       * get FREE_STARTER_CREDITS once
       * never get welcome credits again (idempotent).
   - Existing organisations must NOT receive welcome credits automatically (backfill them as hasReceivedWelcomeCredits = true, but do not add balance).

2) Environment variables:
   - Use:
       FREE_STARTER_CREDITS = 200
       REFERRAL_BONUS_REFERRER = 100
       REFERRAL_BONUS_INVITEE = 100
   - Do not hardcode these values in code – always read from env.

3) Referral behaviour:
   - Referral code is stored on organisation: referralCode (unique).
   - New signups can pass ?ref=CODE or a referralCode field in the registration payload.
   - If referralCode matches an existing organisation:
       * Referrer org: +REFERRAL_BONUS_REFERRER credits
       * New org: +FREE_STARTER_CREDITS + REFERRAL_BONUS_INVITEE credits
   - Make this idempotent by storing referredByOrgId on the invitee org and only granting once.

4) Database / schema:
   - Extend organisations with:
       referralCode (unique)
       referredByOrgId (nullable)
       hasReceivedWelcomeCredits (boolean, default false)
   - Extend credit_transactions type enum with:
       "welcome_bonus"
       "referral_bonus"

5) Existing orgs:
   - Migration should:
       * Generate referralCode for all existing orgs
       * Set hasReceivedWelcomeCredits = true for all existing orgs
       * Do NOT modify their balances.

6) /api/credits and Usage & Billing:
   - Make sure welcome_bonus and referral_bonus transactions appear in the transactions list.
   - In the Usage & Billing tab:
       * If totalPurchasedCredits === 0 and balance > 0, show a message:
         "You’re using welcome or referral credits. Top up to keep AICRM running once they run out."
       * Add a small section that shows:
         "Your referral code: XYZ123" and a "Copy referral link" button using:
         https://aicrm.co.uk/register?ref=XYZ123

7) Safety:
   - Do NOT change the existing Stripe integration or webhook logic (top-up -> topup transaction must keep working as is).
   - Do NOT break the existing free-tier usage logic (AI prompts / emails) yet – just layer credits on top.
   - Add clear console logs when:
       * welcome credits are granted
       * referral bonuses are granted
     with organisation IDs and amounts.

After you implement, summarise:
   - which files changed,
   - any migrations created,
   - any manual steps I need to run (e.g. migration command, env var definitions),
   - and how to manually test:
       1) new org without referral
       2) new org with referral
       3) existing org top-up via Stripe still works.
